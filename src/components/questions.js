export default function Questions({ children }) {
    return (
        <div
            id="questions"
            className="text-center text-3xl p-10 tracking-wider"
        >
            {children}
        </div>
    );
}
